export enum EventType {
    CREATE = "create",
    EXIT = "exit",
    JOIN = "join",
    GET_ME = "get_me",
    INFORMATION = "information",
    MOVE = "move",
    SELECT_COLOR = "select_color",
    STATS = "stats",
}
